<UserControl x:Class="ModernFlyouts.LockKeysControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModernFlyouts" xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d"
             Height="64" d:DesignWidth="364" MaxWidth="364" Margin="0,2,0,0">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundBaseHighBrush}" />
        </Style>
    </UserControl.Resources>
    <Border Background="{DynamicResource FlyoutBackground}" CornerRadius="4">
        <TextBlock x:Name="txt" TextAlignment="Center" FontWeight="SemiBold" VerticalAlignment="Center" Margin="20" FontSize="18" Text="Lock key is on"/>
    </Border>
</UserControl>
